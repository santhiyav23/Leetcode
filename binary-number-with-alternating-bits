class Solution:
    def hasAlternatingBits(self, n: int) -> bool:
        bin1=str(bin(n))
        
        for i in range(len(bin1)-1):
            if bin1[i]==bin1[i+1]:
                return False  


        return True
  
